To compile:
(1) change into the src directory
(2) compile by running
        javac Main.java
(3) copy all of the compiled files up to the top level
    directory (ie, where this file is) of the project with
        cp *.class ..

To run:
(1) make sure you're in the the top level directory of the project
(2) run with
        java Main
